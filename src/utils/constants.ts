type ORDER_STEPS = {
  STEP1: number;
  STEP2: number;
  STEP3: number;
};

type USER_LOGGED = {
  Admin: number;
  Test: number;
};

export const ORDER_STEPS: ORDER_STEPS = {
  STEP1: 1,
  STEP2: 2,
  STEP3: 3,
};

export const USER_LOGGED: USER_LOGGED = {
  Admin: 1,
  Test: 2,
};
